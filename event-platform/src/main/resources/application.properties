spring.application.name=event-platform

# PostgreSQL Connection (Docker ENV vars + Your REAL local fallbacks)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/event_platform}
spring.datasource.username=${DB_USER:ad1tyq}
spring.datasource.password=${DB_PASS:m33tY0urP@dr}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Ensures your data.sql is executed on startup
spring.sql.init.mode=always

# CORS Configuration
cors.allowed.origins=http://localhost:3000
